from setuptools import setup

requires = [
    'fastapi',
    'uvicorn'
]

setup(
    name='apollo',
    version='0.1',
    author='Code R',
    author_email='hello@coderstudio.nl',
    install_requires=requires
)
